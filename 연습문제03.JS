const foodList = ['Pizza', 'Hamburger', 'Chicken', 'Steak'];

// 첫 번째 요소와 마지막 요소를 합침. + 가 문자열 결합을 할때 씀..
const result = foodList[0] + ' ' + foodList[foodList.length - 1];

// log에 결과 출력.
console.log(result);
